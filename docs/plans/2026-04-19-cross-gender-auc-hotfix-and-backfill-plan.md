# Cross-Gender AUC Hotfix and Backfill Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [x]`) syntax for tracking.

**Goal:** Fix the website so missing cross-gender AUCs never display as fake zeros, then compute and ship the real M→F / F→M metrics for the 192-model multi-type release.

**Architecture:** Reuse the existing multi-type aligned cohort pipeline for clinical/lipid/fusion feature spaces, add a gender-transfer evaluation layer inside `multi_type_assets.py`, persist the results into `performance_summary.csv` and `model_metadata.json`, and make the Flask + frontend rendering treat absent values as null/— rather than `0`.

**Tech Stack:** Python, pandas, scikit-learn, xgboost, Flask, pytest, HTML/CSS/JS.

---

## 2026-04-19 Completion Snapshot

- **Status:** Completed, deployed, and regression-verified.
- **Code commit:** `f5f03fc` (`fix: backfill cross-gender auc metrics`)
- **Live URL:** `https://lipid-predict.medaibox.com`
- **Current live release:** `/var/www/lipid-predict/releases/20260419-184105`
- **Root cause confirmed:** cross-gender metrics were absent from exported assets, then backend/frontend rendered missing values as `0`.
- **Implementation result:**
  - `src/multi_indicator_glm5/multi_type_assets.py` now computes and exports real `m2f` / `f2m` metrics;
  - `website/app.py` preserves null instead of coercing missing values to `0`;
  - `website/templates/index.html` renders missing values as `—` instead of fake zeroes.
- **Fresh verification:**
  - `pytest -q` → `68 passed`
  - exported bundle count → `192` models
  - live `/api/models` → `192`
  - representative live values:
    - `BMI_Q_clinical_LR_L2`: `m2f=0.531055900621118`, `f2m=0.5004835589941973`
    - `BMI_Q_fusion_RF`: `m2f=0.735248447204969`, `f2m=0.6344294003868471`
    - `WHR_Q_lipid_RF`: `m2f=0.900689655172414`, `f2m=0.8317829457364341`

## 2026-04-20 Follow-up Hotfix Snapshot

- **Status:** Incremental follow-up hotfix deployed on top of the 2026-04-19 cross-gender release.
- **Current live release:** `/var/www/lipid-predict/releases/20260420-141552`
- **New issue found after launch:** the website `Use Example` payload showed decimal `Gender` values because sample values had been generated by arithmetic mean.
- **Fix shipped:**
  - `src/multi_indicator_glm5/multi_type_assets.py` now exports `Gender` sample values using binary mode rather than mean;
  - `website/app.py` now normalizes legacy decimal `Gender` values to `0/1` before returning sample payloads;
  - `website/trained_models/model_metadata.json` and `outputs/20260419_multi_type_glm5/trained_models/model_metadata.json` have both been backfilled.
- **Fresh verification:**
  - `pytest tests/multi_indicator_glm5/test_multi_type_assets.py tests/website/test_app_display_logic.py -q` → `25 passed`
  - local Flask `/api/sample_data/BMI_Q_fusion_RF` → `Gender=0`
  - live `https://lipid-predict.medaibox.com/api/sample_data/BMI_Q_fusion_RF` → `Gender=0`

## File Map

- **Modify:** `src/multi_indicator_glm5/multi_type_assets.py`
  - Add reusable cross-gender evaluation helpers and persist cross-gender metrics into exported assets.
- **Modify:** `tests/multi_indicator_glm5/test_multi_type_assets.py`
  - Add tests for cross-gender metric computation and metadata export shape.
- **Modify:** `website/app.py`
  - Stop defaulting missing cross-gender metrics to `0`; propagate `None`/null instead.
- **Modify:** `website/templates/index.html`
  - Render missing cross-gender metrics as `—` and avoid false green/red styling on nulls.
- **Modify:** `tests/website/test_app_display_logic.py`
  - Add tests for null cross-gender metrics in API payloads.
- **Modify:** `tests/website/test_frontend_template_polish.py`
  - Add template assertions for null-safe rendering helpers.

---

### Task 1: Lock failing tests for null-safe display

**Files:**
- Modify: `tests/website/test_app_display_logic.py`
- Modify: `tests/website/test_frontend_template_polish.py`

- [x] Add a backend test proving missing `m2f_auroc/f2m_auroc` stays `None` rather than `0`.
- [x] Add a frontend test proving the template contains a null-safe formatter for AUC cells.
- [x] Run `pytest tests/website/test_app_display_logic.py tests/website/test_frontend_template_polish.py -q` and confirm failure.

### Task 2: Lock failing tests for cross-gender export

**Files:**
- Modify: `tests/multi_indicator_glm5/test_multi_type_assets.py`

- [x] Add a helper-level test for male→female / female→male metric computation.
- [x] Add a metadata export test proving `build_metadata_entry()` includes `m2f_auroc`, `f2m_auroc`, and `cross_avg_auroc`.
- [x] Run `pytest tests/multi_indicator_glm5/test_multi_type_assets.py -q` and confirm failure.

### Task 3: Implement the real fix

**Files:**
- Modify: `src/multi_indicator_glm5/multi_type_assets.py`
- Modify: `website/app.py`
- Modify: `website/templates/index.html`

- [x] Implement null-preserving metric serialization in backend + frontend.
- [x] Implement cross-gender evaluation on aligned multi-type subsets using `Gender` (0=male, 1=female).
- [x] Persist cross-gender metrics into `performance_summary.csv`, `trained_models/model_metadata.json`, and website-synced metadata.
- [x] Re-run the targeted pytest set until green.

### Task 4: Re-export, deploy, and verify

**Files:**
- Verify: `outputs/20260419_multi_type_glm5/*`
- Verify: `website/trained_models/model_metadata.json`
- Verify: live deployment

- [x] Run the export pipeline and confirm 192 models remain present.
- [x] Confirm representative models now have real `m2f_auroc/f2m_auroc` values (or null where scientifically uncomputable).
- [x] Deploy to the server, restart the service, and verify the live API/UI no longer show fake zeros.
