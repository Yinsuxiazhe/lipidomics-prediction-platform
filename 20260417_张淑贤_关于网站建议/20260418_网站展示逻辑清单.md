# 2026-04-18 网站展示逻辑清单（2026-04-19 完成状态版）

> **状态口径**：以下清单对应的是 **截至 2026-04-19 的本地当前代码版本**。  
> 训练与网站展示逻辑已经真实补齐；若要对外访问，仍需单独做 GitHub push / 服务器部署。

## 一、本轮展示逻辑已完成项

### 1. indicator 命名
- [x] 前台 indicator 主名称统一显示为变化量写法：`ΔBMI`、`ΔPBF`、`ΔPSM`、`ΔWHR` 等
- [x] 不再把 `q3 / q4 / Q / T` 混进 indicator 主名称
- [x] 不再在同一列混排“全称 + 缩写 + 后缀 + 旧内部命名”

### 2. grouping 展示
- [x] grouping 从 indicator 主列中拆出，单独展示
- [x] 统一前台写法：
  - `Q（Q1 vs Q4）`
  - `T（T1 vs T3）`
- [x] 不再直接向用户暴露旧内部命名 `q3 / q4`

### 3. High / Low Response 解释
- [x] 对负向更优指标（BMI / PBF / WHR / 体重 / 腰围 / 臀围 / BMI z-score）：
  - High Response = 下降更多
  - Low Response = 下降较少，甚至可能升高
- [x] 对正向更优指标（PSM）：
  - High Response = 上升更多
  - Low Response = 上升较少，甚至可能下降

### 4. 页面说明层
- [x] help / glossary / performance summary 明确写出：`Δ = outroll - enroll = 干预后 - 干预前`
- [x] Q / T 被解释为 extreme-group grouping，而不是单个分位点编号
- [x] 结果解释、性能表、帮助页口径已统一

## 二、本轮新增并已完成的多类型展示逻辑
- [x] clinical-only 模型接入
- [x] fusion 模型接入
- [x] 按模型类型动态显示 clinical / lipid 输入区
- [x] clinical-only / lipid-only / fusion 三层模型对比模块
- [x] algorithm comparison 展示模块
- [x] calibration 展示模块
- [x] DCA 展示模块
- [x] 模型卡片、输入区、结果区去掉旧的 “lipid-only / 14 models” 残留文案

## 三、fusion 默认展示规则（已定稿且已落地）
- [x] 每个 `indicator × grouping` 下先做同条件比较
- [x] 优先按同一验证框架下的 AUROC 排序
- [x] AUROC 接近时，再比较 AUPRC、Sensitivity、Specificity、稳健性与模型简洁度
- [x] 若 fusion 并未明显更优，则保留客观对比，不强行写成 fusion 最优
- [x] 当前真实结果中：16 个条件里 fusion 胜出 9 个、lipid 胜出 7 个

## 四、本轮本地验收证据
- [x] `performance_summary.csv` 共 192 行
- [x] `website/trained_models/` 共 192 个模型文件 + metadata
- [x] fresh-process 后端只加载当前 192 个模型，不再混入 legacy `website/models`
- [x] targeted pytest 通过（25 passed）
- [x] 浏览器 smoke 验收截图已保存到 `output/playwright/`

## 五、当前仍未在本清单里宣称完成的事项
- [ ] GitHub clean commit / push
- [ ] 服务器部署
- [ ] 对外访问链接回归确认

## 六、最终一句话原则
> 当前这版网站已经不是“只把命名修对”的半成品，而是把 **训练资产 + 多类型模型 + 动态输入 + 对比展示逻辑** 一起补齐的本地完成版；如果要变成线上可访问版本，只需要继续执行发布动作，而不是再回到功能层返工。
